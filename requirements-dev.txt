# Development dependencies for code quality and type checking

# Type checking
mypy>=1.7.0
types-python-dateutil>=2.8.0

# Linting and formatting
ruff>=0.1.6
robotframework-robocop>=9.0.0

# Testing (already in requirements.txt but included for completeness)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-timeout>=2.1.0
pytest-mock>=3.11.0
pytest-xdist>=3.3.1

# Pre-commit hooks
pre-commit>=3.5.0
